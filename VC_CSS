/* ---------- Root Variables for Theming ---------- */
:root {
    --pink: #ff66b2;
    --navy: #000032;
    --light-blue: #add8e6;
}

/* ---------- Global Body Styling ---------- */
body {
    background: url('code-background.gif') no-repeat center center fixed;
    background-size: cover;
    margin: 0;
    padding: 0;
    color: #fff;
    font-family: 'Rajdhani', sans-serif;
}

/* ---------- Header Section Styling ---------- */
header {
    text-align: center;
    padding: 4rem 1rem 2rem;
    background-color: rgba(0, 0, 50, 0.7); /* semi-transparent navy */
    animation: slideIn 1s ease-out; /* entrance animation */
}

header h1 {
    font-family: 'Rubik Glitch', cursive;
    color: var(--pink);
    font-size: 3rem;
    margin-bottom: 0.5rem;
}

header p {
    font-size: 1.25rem;
    color: var(--light-blue);
    margin-bottom: 1.5rem;
}

/* ---------- Navigation Links ---------- */
nav a {
    color: #fff;
    margin: 0 1rem;
    text-decoration: none;
    font-weight: bold;
    font-size: 1.1rem;
}

nav a:hover {
    color: var(--pink);
    text-shadow: 0 0 5px var(--pink);
}

/* ---------- General Section Styling ---------- */
section {
    background-color: rgba(0, 0, 50, 0.8);
    margin: 2rem auto;
    padding: 2rem;
    border-radius: 1rem;
    width: 90%;
    max-width: 900px;
}

/* ---------- Headings Styling ---------- */
h2 {
    font-family: 'Orbitron', sans-serif;
    color: var(--pink);
    font-size: 2rem;
    margin-bottom: 1rem;
    animation: bounceIn 2s infinite alternate;
}

/* ---------- Unordered List Item Styling ---------- */
ul li {
    margin-bottom: 1rem;
}

/* ---------- Footer Section ---------- */
footer {
    text-align: center;
    padding: 1rem;
    background-color: rgba(0, 0, 50, 0.7);
    font-size: 0.9rem;
}

/* ---------- Animations ---------- */
@keyframes slideIn {
    from { transform: translateY(-100px); opacity: 0; }
    to { transform: translateY(0); opacity: 1; }
}

@keyframes bounceIn {
    from { transform: translateY(0); }
    to { transform: translateY(-10px); }
}

/* ---------- Icon Styling (used in contact or lists) ---------- */
.icon {
    width: 20px;
    vertical-align: middle;
    margin-right: 10px;
    filter: drop-shadow(0 0 3px #ff66b2);
}

/* ---------- Hobbies Section ---------- */
.hobbies-section {
    background-color: rgba(0, 0, 50, 0.8);
    border: 1px solid #fdd0e3;
    box-shadow: 0 0 20px rgba(255, 255, 255, 0.2);
    padding: 30px;
    margin: 40px auto;
    border-radius: 1rem;
    width: 90%;
    max-width: 900px;
    color: #fff;
    text-align: left;
    font-family: 'Rajdhani', sans-serif;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.hobbies-section:hover {
    transform: translateY(-5px);
    box-shadow: 0 0 25px #fdd0e3, 0 0 35px #fdd0e3 inset;
}

.hobbies-section h2 {
    font-size: 2em;
    margin-bottom: 20px;
    color: #fdd0e3;
}

.hobbies-section ul {
    list-style-type: none;
    padding: 0;
    margin: 0;
}

.hobbies-section li {
    font-size: 1.2em;
    margin: 15px 0;
    display: flex;
    align-items: center;
    gap: 0.75rem;
    color: white;
    transition: color 0.3s ease;
}

/* ---------- Contact Section ---------- */
#contact {
    background-color: rgba(255, 255, 255, 0.1); /* translucent white */
    padding: 30px;
    border-radius: 20px;
    margin: 30px auto;
    width: 80%;
    max-width: 700px;
    color: white;
    font-family: 'Segoe UI', sans-serif;
    text-align: left;
    box-shadow: 0 0 20px rgba(255, 255, 255, 0.2);
}

/* Overriding with themed background */
section#contact {
    background-color: rgba(0, 0, 50, 0.8);
    padding: 2rem;
    margin: 2rem auto;
    border-radius: 1rem;
    width: 90%;
    max-width: 900px;
    color: #fff;
}

#contact h2 {
    font-size: 2em;
    margin-bottom: 20px;
    color: #fdd0e3;
}

#contact p {
    margin: 15px 0;
    display: flex;
    align-items: center;
    font-size: 1.1em;
}

#contact a {
    color: #add8e6;
    text-decoration: none;
    margin-left: 10px;
    transition: color 0.3s;
}

#contact a:hover {
    color: #ffffff;
}

/* ---------- Contact Method Card Style ---------- */
.contact-method {
    display: flex;
    align-items: center;
    gap: 1rem;
    background-color: rgba(0, 0, 50, 0.8);
    padding: 1rem;
    border-radius: 1rem;
    box-shadow: 0 0 15px rgba(0,0,0,0.5);
    margin-bottom: 1rem;
    color: white;
    font-size: 1.2rem;
}

.contact-icon {
    width: 30px;
    height: 30px;
}

/* ---------- Download CV Button ---------- */
.download-cv {
    text-align: center;
    margin: 30px auto;
}

.download-cv button {
    background-color: #fdd0e3;
    color: #0c2340;
    font-size: 1.2em;
    font-weight: bold;
    border: none;
    padding: 12px 25px;
    border-radius: 30px;
    cursor: pointer;
    transition: transform 0.3s ease, background-color 0.3s ease;
    box-shadow: 0 4px 15px rgba(255, 255, 255, 0.2);
    font-family: 'Rajdhani', sans-serif;
}

.download-cv button:hover {
    background-color: #add8e6;
    transform: scale(1.05);
}

/* ---------- Projects Section ---------- */
#projects {
    background-image: url('BackgroundCVimage.jpeg');
    background-size: cover;
    background-repeat: no-repeat;
    background-position: center;
    color: white;
    padding: 20px;
    border-radius: 10px;
}
